export interface SlashCommandData {
  name: string;
  description?: string;
  content?: string;
}
export interface SubAgentData {
  name: string;
  description?: string;
  model?: string;
  instructions?: string;
}
