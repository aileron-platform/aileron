# Relay Server Configuration
# These values are used as defaults when building the extension

VITE_RELAY_HOST=localhost
VITE_RELAY_PORT=8000
VITE_RELAY_BASE_PATH=/api/v1/client-browser-relay
WXT_TRUSTED_FRONTEND_ORIGINS=http://localhost:8082
