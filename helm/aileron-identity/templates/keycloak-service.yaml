apiVersion: v1
kind: Service
metadata:
  name: aileron-identity-keycloak
  labels:
    {{- include "aileron-identity.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: aileron-identity-keycloak
  ports:
    - name: http
      port: 8080
      targetPort: http
